# Database connection string (optional - currently using static data)
# DATABASE_URL=postgresql://username:password@localhost:5432/database

# Server port (optional, defaults to 5000)
PORT=5000

# Node environment (set automatically by deployment platform)
NODE_ENV=development
